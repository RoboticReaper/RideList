// This page is for global settings that are not role-specific.
// Using the dashboard header menu doesn't make sense because it has rider/driver switch.
// So although this page should be private, it is in the public folder because it shares the same header menu as the public pages.


export default function SettingsPage() {
    return <div>Settings</div>;
}